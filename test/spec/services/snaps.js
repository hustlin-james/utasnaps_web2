'use strict';

describe('Service: snaps', function () {

  // load the service's module
  beforeEach(module('a1App'));

  // instantiate service
  var snaps;
  beforeEach(inject(function (_snaps_) {
    snaps = _snaps_;
  }));

  /*
  it('should do something', function () {
    expect(!!snaps).toBe(true);
  });
  */
});
