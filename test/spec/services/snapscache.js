'use strict';

describe('Service: snapsCache', function () {

  // load the service's module
  beforeEach(module('a1App'));

  // instantiate service
  var snapsCache;
  beforeEach(inject(function (_snapsCache_) {
    snapsCache = _snapsCache_;
  }));

  /*
  it('should do something', function () {
    expect(!!snapsCache).toBe(true);
  });
  */
});
